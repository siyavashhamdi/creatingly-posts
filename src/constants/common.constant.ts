export const Constant = {
  API_ARTIFICIAL_DELAY_MS: 2000, // Simulated API for response time by milliseconds

  POST_MIN_HEIGHT_PX: 50, // Minimum height that a post can be shown as pixel

  POST_MAX_HEIGHT_PX: 250, // Maximum height that a post can be shown as pixel

  POST_MARGIN_HEIGHT_PX: 10, // Bootom margin of any posts as pixel

  EXTENSION_HEIGHT_PX: 500, // Indicated the height of new data want to be shown as pixel
};
